class WeatherDay:
    """Enum to represent different options for weather pulling.

    Brain's weather puller can pull either today's weather or tomorrow's.
    This Enum represents these options.
    """
    TODAY = 1
    TOMORROW = 2
